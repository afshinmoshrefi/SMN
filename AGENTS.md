# SMN shared agent instructions

Applies to Codex, Claude Code and every authorized coding agent.

Before substantive work, read [docs/AGENT_COORDINATION.md](docs/AGENT_COORDINATION.md), fetch current shared records and acknowledge the relevant task/claim. Follow the same process before resuming, changing direction, integrating or deploying. Private chat and provider memory are not the project handoff.

Read [blog/SUBSCRIPTION_DAILY_RUNBOOK.md](blog/SUBSCRIPTION_DAILY_RUNBOOK.md) for the daily workflow. Preserve TradeWave calculation authority: consume all existing engine mathematics unchanged, never calculate a replacement downstream. Discuss suspected engine bugs with Afshin before changing the engine. Production is read-only under the current SMN authorization; Dev publication retains its existing gates and bounded recovery.

## Environments

- **Dev:** `https://smn-dev.trxstat.com`, a local server on Afshin's network, exposed publicly through a Cloudflare tunnel. If it is unreachable, check the tunnel as well as the server.
- **Production:** `https://seasonalmarketnews.com`, read-only under the current SMN authorization.

Use separate clean worktrees and one owner per concern. Do not silently undo another agent's model/effort or quality decisions. Record objections and seek its evidence-based response through the shared task record. Pending optimization discussions must not interrupt publication on the last verified configuration.

Keep work economical: read relevant records and changed evidence, reuse successful receipts, avoid duplicate research/model runs and use focused checks. Do not reduce article quality or invent peer agreement to save usage.
